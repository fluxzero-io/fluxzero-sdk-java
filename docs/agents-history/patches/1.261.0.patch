--- a/articles/sdk/configuration.md
+++ b/articles/sdk/configuration.md
@@ -30,9 +30,6 @@
 a process/client instance. By default the latter contains a task-ID prefix and a UUID, or just a random UUID when
 there is no task ID. Do not use task identity as an exact client-readiness identity.
 
-Use `fluxzero.serialization.typeAliases` or `FLUXZERO_SERIALIZATION_TYPE_ALIASES` for compatible serialized
-class/package renames. Configure the complete list through the usual property sources; read type aliases for ordering
-with upcasters and programmatic overrides.
 
 `FLUXZERO_NAMESPACE` is app-wide by default. It scopes runtime interactions across messaging, tracking, event store, documents, and search unless a specific operation overrides it.
 
--- a/articles/sdk/serialization.md
+++ b/articles/sdk/serialization.md
@@ -6,7 +6,6 @@
 - When a stored top-level type has a breaking schema change, increment `@Revision`.
 - Ask whether the app has deployed historical data before adding an upcaster.
 - Verify upcasters with `TestFixture.whenUpcasting(...)` before deployment.
-- For a compatible class/package rename, configure a type alias instead of inventing a revision migration.
 - Use fully qualified names for serialized types; `@RegisterType` also supports generated type lookup.
 
 ```java
@@ -32,8 +31,7 @@
 }
 ```
 
-Use a full `Data<JsonNode>` upcaster when a revision migration also changes the type, revision, or format. For a
-pure compatible class/package rename, follow the type-aliases article instead:
+Use a full `Data<JsonNode>` upcaster when changing the type, revision, or format:
 
 ```java
 @Component
--- a/articles/sdk/serialization/registered-type-names.md
+++ b/articles/sdk/serialization/registered-type-names.md
@@ -15,5 +15,5 @@
 `META-INF/io.fluxzero.common.serialization.TypeRegistry` resources when assembling a multi-module application.
 Use unambiguous fully qualified names rather than relying on collision resolution for equal simple names.
 
-Type aliases handle historical class/package renames separately; revision upcasters operate on the historical
-serialized source type. Test the actual message/fixture path when using any abbreviated type lookup.
+Use a `Data<JsonNode>` upcaster when a historical serialized type must change. Test the actual message/fixture
+path when using abbreviated type lookup.
--- a/articles/sdk/serialization/type-aliases.md
+++ /dev/null
@@ -1,50 +0,0 @@
-Use a type alias when a stored or queued class/package name changes but its payload shape and revision remain
-compatible. Use an upcaster for an actual schema/revision migration. Combining an upcaster with an alias is valid:
-keep `@Upcast.type` on the historical source name and let the alias map the result to its current class.
-
-## Prefer application configuration
-
-Put the complete alias list in one `application.properties` value:
-
-```properties
-fluxzero.serialization.typeAliases=com.example.legacy.RenameLabel=com.example.labels.ChangeLabel,com.example.legacy.events.*=com.example.labels.events.*
-```
-
-The conventional environment variable is `FLUXZERO_SERIALIZATION_TYPE_ALIASES`; the compact
-`FLUXZERO_SERIALIZATION_TYPEALIASES` spelling is also supported. An environment value takes precedence over system
-and application properties and replaces the complete list; it does not append entries from lower-priority sources.
-Entries can be separated by commas, semicolons or newlines. Package aliases use `.*` on both sides.
-
-For aliases deliberately owned by application code or a test, configure the builder:
-
-```java
-DefaultFluxzero.builder()
-        .addTypeAlias("com.example.legacy.RenameLabel", "com.example.labels.ChangeLabel")
-        .addPackageAlias("com.example.legacy.events", "com.example.labels.events");
-```
-
-The same calls are available in Kotlin. Programmatic aliases override property aliases with the same source.
-Builder aliases reach the primary serializer, snapshot serializer and serializer-backed document serializer.
-
-## Matching and ordering
-
-- Exact aliases win over package aliases.
-- The longest matching package prefix wins; package boundaries are respected.
-- Aliases may chain, but cycles are rejected during registration.
-- Revision upcasters select the historical serialized type first. Aliases map the resulting type before deserialization,
-  so a class rename does not skip an upcaster registered for that historical FQN.
-
-Aliases apply to serialized envelope types, root and nested polymorphic JSON `@class` values, and JSON-encoded
-metadata read through typed `Metadata.get(...)`. Raw metadata strings remain unchanged.
-
-## Fixture and resource boundaries
-
-`TestFixture` uses the configured aliases for JSON resources. It also offers `registerTypeAlias(...)` and
-`registerPackageAlias(...)`. Non-revisioned `@class` values are resolved before Jackson loads the class; a revisioned
-root preserves the historical type until its upcasters have run. Use `@revision`, not a payload field named `revision`.
-
-Direct untyped `JsonUtils` reads outside the fixture have no ambient application serializer. Supply its resolver through
-`JsonUtils.fromFileWithTypeMapper(...)` with the serializer's `upcastType` when aliases are needed.
-
-Verify exact and package renames, overlapping packages, nested polymorphic values, typed metadata, and a historical
-type that needs both a revision upcaster and an alias. Do not introduce an upcaster solely to rewrite a compatible FQN.
--- a/articles/sdk/testing.md
+++ b/articles/sdk/testing.md
@@ -28,7 +28,7 @@
 JSON fixtures use Fluxzero serialization metadata such as `@class` and `@extends`. Keep JSON examples close to the domain test resources and use them when historical payload shape matters.
 
 Use fully qualified names in `@class`. Use
-`@revision` for historical serialized roots, and type aliases for compatible historical class/package renames.
+`@revision` for historical serialized roots.
 Arrays and NDJSON resources can mix current and revisioned records. Read the focused JSON-fixture and revisioned-JSON
 articles before constructing migration fixtures.
 
--- a/articles/sdk/testing/json-fixtures.md
+++ b/articles/sdk/testing/json-fixtures.md
@@ -13,13 +13,11 @@
 }
 ```
 
-An FQN makes the target explicit. Historical FQNs may be kept
-after a class/package move when the corresponding type alias is configured. A compatible rename uses an alias; an
-actual schema migration uses a revision upcaster. Root and nested `@class` values use the configured resolver.
+An FQN makes the target explicit. Use a `Data<JsonNode>` upcaster when a historical type must change.
 
-Add root-level `@revision` beside `@class` for historical serialized data. The fixture runs the revision upcaster chain
-and then resolves aliases. The field `revision` without `@` is ordinary payload data. Follow the revisioned-JSON article
-for the exact resource contract and the registered-type-names article for annotation processing and packaging.
+Add root-level `@revision` beside `@class` for historical serialized data. The fixture runs the revision upcaster
+chain. The field `revision` without `@` is ordinary payload data. Follow the revisioned-JSON article for the
+exact resource contract.
 
 ## Derive variants with `@extends`
 
--- a/articles/sdk/testing/revisioned-json.md
+++ b/articles/sdk/testing/revisioned-json.md
@@ -12,7 +12,7 @@
 ```
 
 The fixture uses `@class` as `Data.type` and `@revision` as `Data.revision`, removes both markers from the payload,
-runs the revision upcasters, then resolves historical type aliases. The plain `revision: 42` remains application data.
+runs the revision upcasters. The plain `revision: 42` remains application data.
 Do not use a field named `revision` as a substitute for `@revision`.
 
 Test a caster directly with `TestFixture.whenUpcasting(...)`. For a production behavior claim, also pass the historical
@@ -23,8 +23,7 @@
 
 Untyped `JsonUtils.fromFile(...)` and `JsonUtils.fromJson(...)` interpret a revisioned object as `Data<JsonNode>`.
 Explicitly typed overloads retain their declared return type. Use fully qualified names in fixture objects;
-historical data keeps the source identity needed by its upcaster. Direct untyped reads outside a fixture need
-an explicit type mapper when application aliases are required.
+historical data keeps the source identity needed by its upcaster.
 
 ## Arrays and newline-delimited resources
 
--- a/manifest.json
+++ b/manifest.json
@@ -4201,10 +4201,6 @@
           "path": "/docs/sdk/serialization/split-event-upcasters"
         },
         {
-          "description": "Map historical class/package names without inventing schema migrations.",
-          "path": "/docs/sdk/serialization/type-aliases"
-        },
-        {
           "description": "Resolve unique simple names and suffixes and preserve generated module indexes.",
           "path": "/docs/sdk/serialization/registered-type-names"
         },
@@ -4667,10 +4663,6 @@
         {
           "description": "Store supported configuration secrets as encrypted property values",
           "path": "/docs/sdk/configuration/encrypted-properties"
-        },
-        {
-          "description": "Map historical class/package names without inventing schema migrations.",
-          "path": "/docs/sdk/serialization/type-aliases"
         }
       ]
     },
@@ -5095,10 +5087,6 @@
         {
           "description": "Publish and consume deliberately named custom topics",
           "path": "/docs/sdk/messages/custom-topics"
-        },
-        {
-          "description": "Map historical class/package names without inventing schema migrations.",
-          "path": "/docs/sdk/serialization/type-aliases"
         },
         {
           "description": "Resolve unique simple names and suffixes and preserve generated module indexes.",
@@ -6148,41 +6136,6 @@
       ]
     },
     {
-      "path": "/docs/sdk/serialization/type-aliases",
-      "title": "Serialization Type Aliases",
-      "summary": "Map historical class/package names without inventing schema migrations.",
-      "source": "articles/sdk/serialization/type-aliases.md",
-      "symbols": [
-        "type aliases",
-        "fluxzero.serialization.typeAliases",
-        "FLUXZERO_SERIALIZATION_TYPE_ALIASES",
-        "registerTypeAlias",
-        "registerPackageAlias",
-        "addTypeAlias",
-        "addPackageAlias",
-        "upcastType",
-        "Metadata.get"
-      ],
-      "links": [
-        {
-          "description": "Serialization And Schema Evolution",
-          "path": "/docs/sdk/serialization"
-        },
-        {
-          "description": "Configuration",
-          "path": "/docs/sdk/configuration"
-        },
-        {
-          "description": "JSON Fixtures And Custom Message Assertions",
-          "path": "/docs/sdk/testing/json-fixtures"
-        },
-        {
-          "description": "Combine aliases with registered type resolution",
-          "path": "/docs/sdk/serialization/registered-type-names"
-        }
-      ]
-    },
-    {
       "path": "/docs/sdk/serialization/registered-type-names",
       "title": "Registered Type Lookup",
       "summary": "Register classes for type lookup while using fully qualified serialized names.",
@@ -6207,10 +6160,6 @@
         {
           "description": "JSON Fixtures And Custom Message Assertions",
           "path": "/docs/sdk/testing/json-fixtures"
-        },
-        {
-          "description": "Migrate historical class and package names",
-          "path": "/docs/sdk/serialization/type-aliases"
         }
       ]
     },
@@ -6244,10 +6193,6 @@
           "path": "/docs/sdk/serialization"
         },
         {
-          "description": "Resolve historical names after revision upcasting",
-          "path": "/docs/sdk/serialization/type-aliases"
-        },
-        {
           "description": "Verify one-to-many event reconstruction",
           "path": "/docs/sdk/serialization/split-event-upcasters"
         }
